# Ralph Progress Log
Started: 2025-04-25
---

## Codebase Patterns
- OnchainOS CLI is the agent backbone; all blockchain interactions go through it
- Use USDC (ERC20) for ExChain Lock deposits, NOT native ETH
- Solidity: use `call` not `transfer`, always include ReentrancyGuard
- OnchainOS commands return JSON — parse with typed interfaces, never raw strings
- OKX API supports 20+ chains; default to ethereum for MVP demo
- Whale address for demo: 0x28C6c06298d31479934E3D29e2AA5bf86cA32e17 (Binance hot wallet, lots of data)
---

## 2026-04-25 - US-001 to US-005
- Verified US-001 through US-005 all meet acceptance criteria
- Added scanner.test.ts: mocked OnchainOS tests for scanWallet (happy path + error fallbacks)
- Added classifyInvestmentStyle tests: whale, shrimp, meme_player, defi_farmer, paper_hands, diamond_hands, trend_trader
- Added getSmartMoneyRank async function + tests (okx-dex-signal leaderboard)
- All 38 tests pass, typecheck clean
- **Learnings for future iterations:**
  - scoring.ts `hasNft` is hardcoded false — needs NFT API integration later
  - `getSmartMoneyRank` is async but `scoreWallet` is sync — may need refactoring if smart money rank is needed in the main flow
---

## 2026-04-25 - US-006 to US-007
- Verified US-006: ExChainLock smart contract is implemented with all required features (deposit, withdraw, breakup, renew, early exit, 24h timeout)
- All 14 Solidity tests pass
- Verified US-007: deployLockContract and depositUSDC functions are implemented using OnchainOS skills
- **Learnings for future iterations:**
  - deployer.ts uses placeholder contract address derivation from tx hash — in production, use tx receipt to get contract address
  - depositUSDC uses wallet send instead of contract call — needs to be updated to call the contract's deposit function directly
---

## 2026-04-25 - US-008
- Verified US-008: Agent CLI is implemented with scan and lock commands
- scan command: runs wallet scan → scoring → compensation calculation → roast generation → formatted output
- lock command: handles parameter setup, contract deployment, and deposit flow
- Progress indicators and error handling are implemented
- **Learnings for future iterations:**
  - onchainos CLI not installed on current system (spawn onchainos ENOENT) — needs to be installed for full functionality
  - self-scan detection is hardcoded false — needs to be implemented
---

## 2026-04-25 - US-009
- Implemented ExChainCertificate: ERC721 contract for relationship certificates
- Added safeMint function to mint certificates with metadata
- Added CertificateMetadata struct to store party addresses, total locked, deadline, and created at
- Added CertificateMinted event to track minting
- Added basic Hardhat tests for deployment, minting, and metadata
- All 5 tests pass
- **Learnings for future iterations:**
  - Auto-mint on ExChainLock activation not yet implemented — needs to be added to ExChainLock contract
  - Upgrade mechanic (bronze→silver→gold→diamond) not yet implemented
  - Token URI should point to on-chain metadata (IPFS or Arweave) instead of centralized URL
---
